<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sourcepoint.app.v6">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".web.WebConsentTransferTestActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OnlyGdprKotlin" />
        <activity android:name=".MainActivityJava" />
        <activity android:name=".MainActivityAuthId" />
        <activity android:name=".NativeMessageActivity" />
        <activity android:name=".MainActivityViewConsent" />
<!--        <activity android:name=".web.WebConsentTransferTestActivity" />-->
        <activity
            android:name=".LinkActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="network"
                    android:scheme="exampleapp" />
            </intent-filter>
        </activity>
    </application>

</manifest>