<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        Customize your theme here.
        <item name="actionBarStyle">@style/AppTheme.MyActionBar</item>
        <item name="actionMenuTextColor">@color/light_green</item>
        <item name="android:actionMenuTextAppearance">@style/ActionMenuTextStyle</item>
        <item name="android:homeAsUpIndicator">@mipmap/back</item>
    </style>

    <style name="AppTheme.MyActionBar" parent="@style/Widget.AppCompat.Light.ActionBar">
        <item name="android:background">@drawable/tool_bar_bg</item>
        <item name="background">@drawable/tool_bar_bg</item>
        <item name="titleTextStyle">@style/AppTheme.MyActionBar.TitleTextStyle</item>
        <item name="android:layout">@layout/tool_bar</item>
    </style>

    <style name="AppTheme.MyActionBar.TitleTextStyle" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="TextLabel" parent="TextAppearance.Design.Hint">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/hintText</item>
    </style>

    <style name="ActionMenuTextStyle" parent="TextAppearance.Design.Hint">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/hintText</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen_background</item>
    </style>


    <style name="switch.Theme" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/colorPrimary</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">#f1f1f1</item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#42221f1f</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
